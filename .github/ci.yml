name: Run Tests on Push

on: [push, pull_request]

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Download the code
        uses: actions/checkout@v3

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: "3.10"

      - name: Install dependencies
        run: pip install pytest scikit-learn

      - name: Run tests
        run: pytest tests/ -v
```

6. Save the file with `Ctrl+S`

---

## Step 4 — Push the File to GitHub

1. Press `Ctrl+Shift+G` to open Source Control
2. You should see `.github/workflows/ci.yml` listed under **Changes**
3. Click the **+** next to Changes to stage everything
4. Type this commit message:
```
Add CI workflow

